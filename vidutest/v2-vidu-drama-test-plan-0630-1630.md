# Vidu Q3-Drama 模型评测方案 v2.0

**文档编号**：v2-vidu-drama-test-plan-0630-1630
**生成时间**：2026-06-30 16:30 (Asia/Shanghai)
**作者**：评测组（基于 GitHub repo `lishuhang/photos/vidutest` 工作目录资料 + 全网公开报道整理）
**适用对象**：评测执行人 / 软文合作方 / Vidu 客户对接
**测试模型**：Vidu Q3-Drama（model_id: `vidu q3-drama`，1080P，28 积分/秒，0.875 元/秒）
**测试入口**：https://germproof-barred-unwilling.ngrok-free.dev/（需 API Key + 测试积分）
**API Key 管理页**：https://platform.vidu.cn/api-keys
**官方文档**：https://platform.vidu.cn/docs/introduction

---

## 0. 文档说明

本文档包含两套评测方案：

- **方案 A**：客观第三方独立评测方案（诚实、真实、可复现）
- **方案 B**：与 Vidu 软文合作稿撰写方案（扬长避短、合规、保留事实底线）

如模型本身质量过硬，两版差异主要在叙事角度；如模型存在短板，方案 B 会主动规避或转换为亮点叙述，方案 A 则如实记录。两版共享同一套测试用例库和同一份"使用说明"，避免重复劳动。

文末附：(1) ngrok 测试站使用说明；(2) 测试用例提示词速查表；(3) 评测日历。

**v2.0 更新**：§4 ngrok 测试站使用说明基于实际 HTML 源码重写，新增短剧/MV 双 Tab 完整操作流程、4 个 API 端点、Python 批量测试脚本

---

## 1. 背景与研究综述

### 1.1 模型定位（基于 vidu_q3_drama.html）

Vidu Q3-Drama 是生数科技面向"精品剧/短剧生产"场景推出的深度优化模型，核心定位：

| 维度 | 官方卖点 |
|------|----------|
| 一致性 | 主体位置关系更稳定，多角色不串位 |
| 动效 | 动态逻辑自然，切镜后镜头连贯；大场面特效冲击力强 |
| 细节&美学 | 画质/光影优于通用模型；人物面部真实质感提升、磨皮感降低；情绪表情生动；音效明显提升 |
| 生产效率 | 批量输入、分段规划、分镜批量输出；时长限制 2-15s 可智能切镜 |
| 性价比 | 28 积分/秒（0.875 元/秒），号称实际价为 Seedance 2.0 的 50%、刊例价的 80% |

主推题材：现代都市、仙侠玄幻、悬疑动作、古装宫斗四类。

### 1.2 公司背景（基于公开报道）

- **生数科技**：2023 年初由清华朱军教授创立，2024-04 推出 Vidu，是国内首家发布视频生成模型的公司
- **融资**：2025 年 A+ 轮超 6 亿（中关村领投），2026-04 B 轮近 20 亿（阿里云领投）
- **Q 系列演进**：Q1（2025-04，VBench 双榜第一）→ Q2（"演技生成"跃迁）→ Q3（2026-01-30，全球首个 16s 声画同出）
- **榜单成绩**：Artificial Analysis 评分 1241，中国第一、全球第二，超越 Runway Gen-4.5、Veo 3.1、Sora 2

### 1.3 竞品格局

| 竞品 | 厂商 | 关键差异化 |
|------|------|-----------|
| Seedance 2.0 | 字节跳动 | 统一多模态音视频联合生成架构，文/图/音/视四模态输入；2026-02 发布，"导演感"标签 |
| Kling 3.0 | 快手 | 1080P 电影级，2026 国产评测登顶 |
| 即梦 AI | 字节 | 性价比+易用，日均生成 500 万条视频 |
| 万兴剧厂 | 万兴 | 集成 Vidu+Kling+即梦多模型，与 Vidu 官方合作，AI 真人剧 Agent 分镜一抽可用率 70% |
| Sora 2 | OpenAI | 已于 2026-03 停服，让出市场空间 |

### 1.4 公开评测中已暴露的注意点

| 来源 | 注意点 |
|------|--------|
| 知乎深度实测 | 复杂场景（如"无量空处"）背景复杂度拉满时偶有马赛克闪烁 |
| 量子位实测 | 6 大特效（粒子/流体/动力学/运镜/转场/光影）需自然语言触发，提示词写法敏感 |
| 腾讯云实测 | Q1→Q2→Q3 差异显著，但 Q3 在某些场景反而不如 Q2（待复现） |
| 知乎国产评测打分 | Vidu 综合评分 4.9/10，主要扣分项"面部变化明显"；同测评中即梦 5.8/10、MiniMax 在 IP 连续短剧上更稳 |
| YouTube 海外测评 | 角色一致性是核心卖点也是核心风险，参考图质量决定上限 |
| 飞书评论 | 短视频平台"剧情抓人但演员不对劲"现象 |

### 1.5 内部讨论关键点（discussion.txt）

- 客户 Vidu 暂未明确交付物，仅要"测试意见"
- 历史教训：上次阿里某模型测试方法 OK 但效果不行（"欢乐马"模型人脸偏油）
- 同事"虎皮尖椒"建议测试方向：
  1. 人物一致性
  2. 场景道具一致性
  3. 长集数稳定性（10 集以上不降智）— 积分和时间上较难
  4. 人物口型对齐
- 提示词可用大模型优化
- "脸油"问题多源于垫图本身油腻，自生成参考图可改善

### 1.6 提示词指南要点（prompt_guide.html）

- 三类剧本提示词结构：表情戏（时间维度+具体细节）、双人对峙戏（过肩镜头+交替镜头）、打戏（11 字段 JSON 模板）
- **负面提示词基本失效**（AI 靠统计关联学习，"不"是噪音）→ 改写为"描述能看到什么"
- 最佳长度 50-150 字
- 一致性三原则：角色（固定外貌描述）、镜头（焦段+景别+运镜）、风格（参考图+色调+灯光）
- 五句口诀：不滥用"不要"、大动作拆小、静态加微动作、固定结构、全程统一

---

## 2. 方案 A：客观第三方独立评测方案

### 2.1 评测原则

1. **可复现**：每个测试用例附完整 prompt、参考图说明、参数、生成时间、积分消耗
2. **公平对照**：同一提示词、同类参考图同时跑 Vidu Q3-Drama 和至少一款竞品（建议 Seedance 2.0 + Kling 3.0）
3. **不预设立场**：既不刻意找茬，也不回避问题
4. **承认局限**：积分/时间有限，10 集长程稳定性测试可能无法完整覆盖，需明确标注
5. **数据说话**：主观感受配以可量化指标（如面部一致度用 IP-Adapter score，口型对齐用 SyncNet 分数）

### 2.2 评测维度与权重

| 维度 | 权重 | 评测方法 |
|------|------|----------|
| 角色一致性（跨镜头/跨集） | 25% | 同一角色 5 镜头 + 3 集复测，人工标注 + CLIP-IQA |
| 场景道具一致性 | 15% | 同场景 3 镜头 + 关键道具出现/消失/变形计数 |
| 镜头叙事与切镜逻辑 | 15% | 复杂多镜脚本（5+ 切镜）成功率，镜头方向/景别是否符合提示词 |
| 动效与物理合理性 | 10% | 打戏/特效场景，人物动作幅度、物体运动轨迹、碰撞反馈 |
| 口型对齐 | 10% | Speech 提示词生成，人工评估 + SyncNet 离线打分（如可下载视频） |
| 画面美学（质感/光影/磨皮） | 10% | 与竞品同提示词盲测，10 人评分（去除最高最低） |
| 音效与氛围 | 5% | 音视频直出场景，BGM/环境音/对白三类分别评估 |
| 性价比与生产效率 | 5% | 单秒积分、生成耗时、批量输入支持、失败重试成本 |
| 长程稳定性（如积分允许） | 5% | 10 集连续生成，统计"降智"曲线 |

### 2.3 测试用例库（共 24 个，按 4 题材 × 6 维度设计）

> 完整提示词见附录 B。下表为索引。

| 编号 | 题材 | 维度 | 难度 | 说明 |
|------|------|------|------|------|
| T01 | 现代都市 | 角色一致性 | ★ | 单人 3 镜头对话 |
| T02 | 现代都市 | 双人对峙 | ★★ | 过肩镜头交替 |
| T03 | 现代都市 | 口型对齐 | ★★★ | Speech 短句对白 |
| T04 | 现代都市 | 道具一致性 | ★★ | 手机/咖啡杯多次出现 |
| T05 | 现代都市 | 情绪变化 | ★★★ | 微表情梯度（皱眉→震惊→笑） |
| T06 | 现代都市 | 镜头叙事 | ★★★★ | 5 镜头办公室桥段 |
| T07 | 仙侠玄幻 | 角色一致性 | ★★★ | 古装服饰跨镜头 |
| T08 | 仙侠玄幻 | 大场面特效 | ★★★★ | 火球→火蝶→火凤 |
| T09 | 仙侠玄幻 | 多主体一致性 | ★★★★ | 3 人混战不串位 |
| T10 | 仙侠玄幻 | 动效物理 | ★★★★ | 法术碰撞冲击波 |
| T11 | 仙侠玄幻 | 镜头叙事 | ★★★★★ | 5 镜头史诗级打戏 |
| T12 | 仙侠玄幻 | 音效氛围 | ★★★ | 法术音+环境音+BGM |
| T13 | 悬疑动作 | 动作逻辑 | ★★★★ | 枪战/追逐/打斗 |
| T14 | 悬疑动作 | 物理反馈 | ★★★ | 撞击/碎裂/扬尘 |
| T15 | 悬疑动作 | 镜头跟随 | ★★★ | 手持跟拍稳定度 |
| T16 | 悬疑动作 | 表演感 | ★★★ | 紧张表情 + 喘息 |
| T17 | 古装宫斗 | 服饰细节 | ★★★ | 凤冠霞帔纹理 |
| T18 | 古装宫斗 | 表演感 | ★★★★ | "嫌弃"等复杂情绪 |
| T19 | 古装宫斗 | 双人对话 | ★★ | 主仆对峙 |
| T20 | 古装宫斗 | 多人场景 | ★★★★ | 宴会 5+ 人物站位 |
| T21 | 跨题材 | 长程稳定性 | ★★★★★ | 10 集连续生成（如积分允许） |
| T22 | 跨题材 | 负面提示词失效验证 | ★★ | "不戴帽子"对照 |
| T23 | 跨题材 | 提示词长度敏感度 | ★★ | 30/80/150/300 字对照 |
| T24 | 跨题材 | 参考图质量影响 | ★★★ | 高清自拍 vs 网图 vs 自生成图对照"脸油"问题 |

### 2.4 执行流程

```
Day 1（半天）
├─ 申请 API Key + 联系 Vidu 同学发积分
├─ 探通 ngrok 测试站，跑通 1 个最简用例（T01）
├─ 校准竞品（Seedance 2.0 / Kling 3.0）的同款用例
└─ 记录基线参数（时长/分辨率/默认设置）

Day 2
├─ 跑 T01-T12（现代都市 + 仙侠玄幻）
├─ 每个用例同时跑 Vidu + 1 款竞品
├─ 立即人工标注 + 截图存档
└─ 当晚汇总异常用例

Day 3
├─ 跑 T13-T20（悬疑动作 + 古装宫斗）
├─ 跑 T22-T24（跨题材验证）
└─ 启动 T21 长程测试（如积分允许）

Day 4
├─ T21 长程测试续跑 + 中期降智曲线分析
├─ 异常用例复测（每个 2 次重抽）
└─ 数据汇总

Day 5
├─ 撰写评测报告（基于本方案 A 模板）
├─ 内部交叉评审
└─ 输出给 Vidu 的"测试意见"清单
```

### 2.5 数据记录模板

每个用例必须填表：

| 字段 | 示例 |
|------|------|
| 用例编号 | T08 |
| 题材 | 仙侠玄幻 |
| 测试维度 | 大场面特效 |
| 提示词（完整） | 见附录 B-T08 |
| 参考图（说明） | 图1 古风男子半身像（自生成 1080P）；图2 长袍反派（自生成）；图3 长刀道具图 |
| 模型 | vidu q3-drama |
| 参数 | 1080P, 8s, 智能切镜 ON |
| 生成时间 | 142s |
| 积分消耗 | 224（8s × 28） |
| 输出视频 URL | （本地存档路径） |
| 主观评分（1-10） | 7 |
| 一致性（CLIP-IQA） | 0.812 |
| 物理合理性（1-5） | 4 |
| 口型 SyncNet（如有） | N/A |
| 异常现象 | 火蝶阶段出现 1 帧闪烁；火凤撞击后场景过渡稍快 |
| 对照竞品 | Seedance 2.0：火凤无撞击景观，5/10；Kling 3.0：未跑 |
| 备注 | 提示词按 prompt_guide 表情戏模板优化过 |

### 2.6 已知风险与对策

| 风险 | 对策 |
|------|------|
| 积分不足无法跑 T21 长程测试 | 改为 3-5 集抽样，明确标注"长程稳定性未充分测试" |
| ngrok 站点不稳定 | 提前备份所有 prompt + 参考图，本地存档所有输出 |
| "脸油"问题复现 | T24 专门做对照，参考图分三组：网络人像 / 自生成 / 高清自拍，记录差异 |
| 竞品 API 限额 | 优先保证 Vidu 用例完整，竞品用例降级为"同一提示词公开 demo 截图对比" |
| 提示词写法导致结果偏差 | 所有提示词先经 GPT-4o / Claude 优化，按 prompt_guide 规范化 |

### 2.7 评测报告输出结构（方案 A）

1. 评测摘要（1 页，含综合评分表）
2. 测试环境与参数
3. 各维度详细评测（含 24 用例数据 + 竞品对比）
4. 优势确认（明确可信赖的卖点）
5. 问题清单（按严重程度排序）
6. 改进建议（给 Vidu 的具体可执行建议）
7. 局限性声明（积分/时间/提示词写法等）
8. 附录：完整提示词库 + 参考图 + 输出视频存档链接

---

## 3. 方案 B：与 Vidu 软文合作稿撰写方案

### 3.1 合作定位

Vidu 是客户，本次合作目标：

- 客户未明确要求"交付什么"，仅要"测试意见" → 实际产出弹性大
- 历史教训：上次阿里某模型我们测完发现效果不行，客户对外宣传时引用了我们的"测试方法 OK"评价，导致后续舆论反噬
- 本次策略：**测试意见私下交付 Vidu，对外发布稿件保留事实底线但叙事角度向 Vidu 倾斜**

### 3.2 软文核心叙事框架

**主标题候选**（择一）：
1. 《为"剧"而生：实测 Vidu Q3-Drama，看国产 AI 视频如何把短剧生产成本打下来》
2. 《28 积分/秒的代价：用 Vidu Q3-Drama 跑了 24 个分镜后的真实体验》
3. 《从"脸油"到"演技"：Vidu Q3-Drama 把 AI 短剧从 demo 推到生产线》

**核心叙事三段式**：

1. **行业痛点段** — AI 短剧"剧情抓人但演员不对劲"、参考图脸油、长程降智、特效出戏
2. **Q3-Drama 破局段** — 28 积分/秒定价 + 智能切镜 + 多主体一致性 + 音视频直出
3. **实测验证段** — 24 用例中表现优秀的部分（择优展示）+ 改进空间以"未来可期"措辞呈现

### 3.3 扬长避短策略矩阵

| 模型实际表现 | 软文处理方式 |
|------------|------------|
| 表现优秀（如性价比、大场面特效、音效直出） | 重点着墨，配实测截图 + 数据 |
| 表现中等（如角色一致性，Q3 强但非绝对领先） | 客观陈述优势，提及"仍在持续优化" |
| 表现较弱（如某些复杂场景闪烁、长程稳定性未充分验证） | 不主动提及；若被读者问到，以"测试版/仍在迭代"回应 |
| 已知风险（脸油问题） | 主动归因到"参考图质量"，给出"自生成参考图"解决方案，转化为"使用技巧"亮点 |
| 与竞品对比 | 不点名 Seedance 2.0/Kling，用"友商主流模型"代称；对比时仅展示 Vidu 优势维度 |

### 3.4 必须保留的事实底线（不可让步）

1. **不夸大榜单成绩**：可引用 Artificial Analysis 中国第一/全球第二，但必须标注"截至 2026-01-30 评分"
2. **不虚构案例**：所有展示视频必须真实生成，标注提示词与参数
3. **不抹黑竞品**：不出现"碾压""秒杀"等贬低性措辞
4. **不隐瞒局限**：长程稳定性若未充分测试，稿件中不写"已验证可稳定生成 10 集"
5. **不伪造用户证言**：引用第三方评论须注明来源链接

### 3.5 软文稿件结构（建议 2500-3500 字）

```
标题（择一）
副标题：基于 24 个分镜用例的实测体验

导语（200 字）
- 行业背景：AI 短剧风口、Sora 退场留下的市场空间
- 测试缘起：拿到 Vidu Q3-Drama 测试权限
- 核心结论预告

一、为什么短剧场景需要专门的"drama 模型"（400 字）
- 通用视频模型的三个短板：一致性、镜头叙事、音画同步
- Vidu Q3-Drama 的差异化定位
- 28 积分/秒的定价意味着什么（对比行业内主流模型）

二、实测：四类题材 24 个分镜（1200 字，核心）
- 现代都市：办公室桥段（T06）展示多镜头叙事能力
- 仙侠玄幻：火凤撞击（T08）展示大场面特效
- 悬疑动作：追逐戏（T13-T15）展示动效物理
- 古装宫斗：嫌弃表情（T18）展示情绪表演
- 每个案例配：提示词 + 参考图缩略 + 输出视频截图 + 1-2 句点评
- 选择性展示：仅展示 Vidu 表现达 7/10 以上的用例

三、使用技巧：把 Q3-Drama 用对的方法（500 字）
- 参考图自生成（解决"脸油"问题的根本方案）
- 提示词按表情戏/双人对峙/打戏三类模板写
- 负面提示词失效 → 改用"描述能看到什么"
- 音频提示词放末尾

四、与友商主流模型的横向参考（300 字）
- 不点名，用"友商 2.0""友商 3.0"代称
- 仅对比 Vidu 优势维度：性价比、大场面特效、多主体一致性
- 提及"各有所长，建议按场景选择"

五、改进期待（200 字）
- 长程稳定性（10 集以上）的验证期待
- 复杂场景细节稳定性的优化空间
- 以"测试版体验，正式版可期"措辞收尾

结语（100 字）
- Vidu Q3-Drama 把 AI 短剧从"demo 阶段"推到了"可量产阶段"
- 28 积分/秒的定价让小团队也能上手
- 期待正式版

附：测试用例索引 + 提示词库链接
```

### 3.6 软文 Brief 模板（给写手）

```
【稿件定位】实测体验稿，非硬广
【字数】2500-3500 字
【目标读者】AI 短剧创作者、AIGC 工作室、内容平台运营
【发布平台】微信公众号 + 知乎专栏 + B 站动态
【时效】Vidu Q3-Drama 测试期间内发布
【必须包含】
  - 28 积分/秒定价 + 0.875 元/秒
  - Artificial Analysis 中国第一/全球第二（标注日期）
  - 4 类题材至少各 1 个实测案例
  - 至少 1 个"使用技巧"段落
【必须避免】
  - 点名贬低具体竞品
  - 使用"碾压""秒杀""完爆"等措辞
  - 虚构未实测的场景效果
  - 引用未注明来源的第三方数据
【配图要求】
  - 至少 8 张：4 题材各 1 张输出截图 + 1 张价格对比图 + 1 张提示词模板图 + 1 张榜单截图 + 1 张工作流示意图
  - 所有视频截图须标注提示词与生成参数
【交付物】
  - Markdown 源文件
  - 配图原图
  - 提示词库 Excel
  - Vidu 内部测试意见（单独文件，不对外发布）
```

### 3.7 与方案 A 的关系

- **方案 A 是方案 B 的事实基础**：方案 B 中所有"实测案例"必须来自方案 A 的真实测试数据
- **方案 A 的"问题清单"不进方案 B**：以"内部测试意见"形式单独交付 Vidu
- **方案 B 的"改进期待"措辞**：从方案 A 的"问题清单"中筛选低严重度项，转换为"未来可期"措辞
- **如方案 A 发现重大问题**（如长程降智严重、参考图脸油无法解决）：方案 B 暂缓发布，先与 Vidu 沟通
- **如方案 A 整体表现优秀**：方案 A 与方案 B 措辞趋同，差异仅在叙事角度

---

## 4. ngrok 测试站使用说明

> ✅ 本节基于实际抓取的 ngrok 测试站 HTML 源码（39 KB，2026-06-30 由用户保存到 GitHub repo `lishuhang/photos/vidutest/view-source.ngrok.html`）编写，UI 元素与 API 端点均经过源码核对。

### 4.1 站点概览

- **访问地址**：https://germproof-barred-unwilling.ngrok-free.dev/
- **页面标题**：`Vidu 体验平台`
- **副标题**：`AI 驱动的视频生成 — 短剧生成 & MV 生成，一站式体验`
- **技术栈**：原生 HTML + Vanilla JS（单文件 SPA，无 React/Vue），暗色主题（深蓝紫底 + 紫色/青色辅色）
- **存储**：浏览器 localStorage（键 `vidu_api_key` / `sp_demo_tasks` / `mv_demo_tasks`），无服务端 session，刷新页面不丢
- **鉴权**：仅靠 Vidu API Key（无账号系统、无登入登出、无余额面板）

### 4.2 前置准备

1. **申请 API Key**
   - 访问 https://platform.vidu.cn/api-keys
   - 点击「新建 API Key」
   - 复制生成的 Key ID（形如 `vda_xxxxx`）
   - 联系 Vidu 同学（在测试群内）发放测试积分

2. **积分到账确认**
   - 在 platform.vidu.cn 个人中心查看积分余额
   - 测试期间建议保持 ≥ 12000 积分（覆盖 24 用例 + 重抽 + T21 长程测试）

3. **网络环境**
   - ngrok-free.dev 域名国内可直连，无需翻墙
   - 首次访问浏览器会显示 ngrok 警告页"You are about to visit"，点击"Visit Site"即可
   - 该站点无服务端渲染，所有内容由 JS 在浏览器端动态生成

### 4.3 页面布局

页面单列居中（max-width 900px），自上而下顺序：

1. **Hero 区**：大标题"Vidu 体验平台" + 副标题
2. **API Key 横条**：单行（label + 密码输入框 + 状态指示灯，灰=未填 / 绿=已填）
3. **Tabs 切换条**：两个 Tab 按钮
   - `短剧生成`（默认激活）
   - `MV 生成`
4. **当前 Tab 内容**：3 张卡片
   - 短剧 Tab：`创建短剧任务` / `任务列表` / `任务详情`
   - MV Tab：`创建 MV 任务` / `MV 任务列表` / `MV 详情`

### 4.4 短剧生成流程（与 Q3-Drama 评测主路径）

#### 步骤 1：填写 API Key
- 在顶部 `Vidu API Key` 输入框粘贴 `vda_xxxxx`
- 状态灯从灰变绿
- Key 自动存入 localStorage，下次访问免输

#### 步骤 2：填写剧本
- `剧本名称（20字符内）`：必填，例如"典当行退婚"
- `剧本内容（2000中文字符内）`：必填，粘贴分镜剧本（建议按 prompt_guide 的"打戏 JSON 模板"或"表情戏时间维度"格式编写）

#### 步骤 3：上传素材
- **拖拽或点击**`角色/场景素材`区域上传图片
- 支持格式：png / jpeg / jpg / webp
- 支持多选
- 上传后每个素材出现一行：
  - 缩略图
  - `名称`输入框
  - `类型`下拉框（character / scene / tool）
  - `描述`输入框
  - `删除`按钮
- **CSV 批量导入**（可选）：点击"支持 CSV 批量导入"按钮选 .csv 文件，表头需含 `id,type,name,image_uri,description` 五列，`image_uri` 为图片 URL

#### 步骤 4：设置参数

| 参数 | 选项 | 默认值 | 说明 |
|------|------|--------|------|
| quality | high / standard | high | 画质 |
| placement | upload / panorama / step | upload | 站位方式 |
| aspect_ratio | 9:16 / 16:9 | 9:16 | 输出比例（竖屏短剧建议 9:16） |
| duration | 0=自动 / 2~30s | 0 | 单镜时长（秒）；0 让模型自动规划 |
| style | 自由文本（≤30 字） | 真人写实 | 风格描述 |
| 站位参考图 URL | 可选 | 留空 | 不上传则不使用 |

> ⚠️ **重要发现**：UI 上未暴露 `resolution` 选项，但代码内**硬编码 1080p**。这与官方文档"vidu q3-drama 1080P 28 积分/秒"一致。

#### 步骤 5：发起任务
- 点击 `发起任务` 按钮
- 日志区显示：
  - `创建任务: {剧本名}...`
  - 成功：`[ok] task_id: {taskId}`
  - 失败：`[error] {错误信息}`
- 任务自动入"任务列表"表格（含 # / 剧本名 / task_id / 状态 / 积分 / 操作）

#### 步骤 6：查询状态
- **单条查询**：点该行 `查询` 按钮
- **批量查询**：点表格上方 `批量查询` 按钮，一次性轮询所有任务

任务状态颜色映射：
| 状态 | 颜色 | 含义 |
|------|------|------|
| pending | 金色 #fbbf24 | 排队中 |
| processing | 青色 #67e8f9 | 生成中 |
| success | 绿色 #34d399 | 成功（可查看详情） |
| failed | 红色 #f87171 | 失败（详情会显示 err_code / err_msg） |

#### 步骤 7：查看详情
- 状态变为 `success` 后点该行 `详情` 按钮（`failed` 状态也可查看，会显示错误信息）
- "任务详情"卡片展开：
  - **状态横幅**：state 文本 + 扣除积分
  - **成片视频**：`<video controls>` 播放器，URL 来自 `derived.signed_final_video_url`
  - **分镜网格**：每个 shot-card 含
    - 编号（场X-镜X）
    - 时长
    - 分镜视频播放器
    - prompt 文本
  - **原始 JSON**：折叠面板（`<details>` + `<pre>`），最大高度 280px

#### 步骤 8：导出与清理
- `导出 CSV`：下载任务列表，表头 `task_id,script_name,state,scene_no,shot_no,duration_sec,video_prompt,signed_video_url`，带 BOM（Excel 友好）
- `清空`：清空本地任务列表（弹窗"确定清空？"确认）

### 4.5 MV 生成流程（评测方案 B 软文素材的额外亮点）

> MV 生成是测试站提供的第二条路径，不在 24 用例库主路径中，但可作为软文合作稿的"差异化亮点"展示（Vidu Q3 不只是短剧，还能做音乐视频）。

#### 步骤 1：切换 Tab
- 点击顶部 `MV 生成` Tab 按钮

#### 步骤 2：上传素材
- **模特/风格图片**：1~7 张，png/jpeg/jpg/webp，单张 ≤ 50MB
- **音频文件**：1 个，mp3/wav/aac/m4a，10~180 秒，≤ 20MB
- 拖拽或点击上传区均可

#### 步骤 3：设置参数

| 参数 | 选项 | 默认值 | 说明 |
|------|------|--------|------|
| aspect_ratio | 1:1 / 16:9 / 9:16 / 4:3 / 3:4 | 16:9 | 输出比例 |
| resolution | 540p / 720p / 1080p | 720p | 清晰度 |
| quality | high / standard | high | 画质 |
| language | 自动 / zh / en | 自动 | 音频语言 |
| prompt | ≤ 3000 字 | — | 文本描述（可选） |
| style | 自由文本 | — | 如"弥散风格、二次元风格" |
| lip_sync | 复选框 | 关 | 对口型，每秒 +4 积分，仅真人正面近景 |
| add_subtitle | 复选框 | 关 | 添加字幕 |
| lip_ref_url | URL 或本地上传 | — | 对口型参考人脸图（开 lip_sync 时必填） |
| subtitle_color | HEX 颜色 | #FFFFFF | 字幕颜色（开 add_subtitle 时有效） |
| srt_url | URL 或本地上传 .srt/.vtt/.ass | — | 字幕文件（开 add_subtitle 时可填） |
| callback_url | URL | — | 任务状态变化时回调（可选） |

#### 步骤 4：发起任务
- 点击 `发起 MV 任务` 按钮
- 日志区显示：
  - `正在读取文件并编码...`（图片/音频转 base64）
  - `正在提交任务...`
  - 成功：`[ok] task_id: {taskId}`

#### 步骤 5：查询与详情
- 同短剧流程（批量查询 / 单条查询 / 详情展开）
- 详情卡片包含：成片视频（`signed_url`） + 分镜片段网格（`job_records[].jobs[]`）+ 折叠 JSON
- 若失败，红色文本展示 `错误: {err_code} - {err_msg}`

### 4.6 API 端点（用于脚本化批量测试）

测试站后端暴露 4 个 API（前端 fetch 调用，可通过抓包或直接请求复用）：

#### 短剧 API

**创建短剧任务**
```
POST /api/test/create
Content-Type: application/json

{
  "api_key": "vda_xxxxx",
  "payload": {
    "script_name": "典当行退婚",
    "script_content": "...",
    "assets": [
      {"name": "苏文博", "type": "character", "image_uri": "data:image/png;base64,...", "description": "..."}
    ],
    "quality": "high",
    "placement": "upload",
    "duration": 0,
    "resolution": "1080p",
    "aspect_ratio": "9:16",
    "style": "真人写实",
    "upload_image_url": ""
  }
}
```

**查询短剧任务**
```
GET /api/test/query?id={task_id}&api_key={key}
```

返回字段：
- `task.meta.state`：pending / processing / success / failed
- `task.derived_data.shots[]`：分镜列表（含场X-镜X 编号、时长、video URL、prompt）
- `task.derived_data.signed_final_video_url`：成片视频 URL
- `task.derived_data.deducted_credits`：扣除积分

#### MV API

**创建 MV 任务**
```
POST /api/mv/create
Content-Type: application/json

{
  "api_key": "vda_xxxxx",
  "payload": {
    "images": ["data:image/png;base64,..."],
    "audio_url": "data:audio/mp3;base64,...",
    "aspect_ratio": "16:9",
    "resolution": "720p",
    "quality": "high",
    "language": "",
    "prompt": "...",
    "style": "...",
    "lip_sync": false,
    "add_subtitle": false,
    "lip_ref_url": "",
    "subtitle_color": "#FFFFFF",
    "srt_url": "",
    "callback_url": ""
  }
}
```

**查询 MV 任务**
```
GET /api/mv/query?id={task_id}&api_key={key}
```

返回字段：
- `state` / `err_code` / `err_msg`
- `signed_url`：成片视频 URL
- `job_records[].jobs[].signed_url`：分镜视频 URL

### 4.7 Python 批量测试脚本（基于真实 API）

```python
import requests
import base64
import json
import time
from pathlib import Path

BASE = "https://germproof-barred-unwilling.ngrok-free.dev"
API_KEY = "vda_969457082661089280_Le2wQG1I2cceRqSW8OB4yJ5yeZdet1Ky"
HEADERS = {
    "ngrok-skip-browser-warning": "1",  # 绕过 ngrok 警告页
    "User-Agent": "Mozilla/5.0",
    "Content-Type": "application/json"
}

def image_to_data_uri(path):
    """本地图片转 data URI（与测试站前端行为一致）"""
    p = Path(path)
    suffix = p.suffix.lower().lstrip(".")
    mime = {"png": "image/png", "jpg": "image/jpeg", "jpeg": "image/jpeg", "webp": "image/webp"}[suffix]
    b64 = base64.b64encode(p.read_bytes()).decode("ascii")
    return f"data:{mime};base64,{b64}"

def create_drama_task(script_name, script_content, assets, duration=0,
                     quality="high", placement="upload", aspect_ratio="9:16",
                     style="真人写实", upload_image_url=""):
    """创建短剧任务
    assets: [{"name":"苏文博","type":"character","image_path":"/path/x.png","description":"..."}]
    """
    payload = {
        "script_name": script_name,
        "script_content": script_content,
        "assets": [
            {
                "name": a["name"],
                "type": a["type"],
                "image_uri": image_to_data_uri(a["image_path"]),
                "description": a.get("description", "")
            } for a in assets
        ],
        "quality": quality,
        "placement": placement,
        "duration": duration,
        "resolution": "1080p",  # 测试站硬编码
        "aspect_ratio": aspect_ratio,
        "style": style,
        "upload_image_url": upload_image_url
    }
    r = requests.post(
        f"{BASE}/api/test/create",
        headers=HEADERS,
        json={"api_key": API_KEY, "payload": payload},
        timeout=60
    )
    return r.json()

def query_task(task_id):
    r = requests.get(
        f"{BASE}/api/test/query",
        headers=HEADERS,
        params={"id": task_id, "api_key": API_KEY},
        timeout=30
    )
    return r.json()

def poll_until_done(task_id, max_wait=600, interval=15):
    """轮询直到完成或超时"""
    start = time.time()
    while time.time() - start < max_wait:
        r = query_task(task_id)
        task = r.get("task", {})
        state = task.get("meta", {}).get("state", "unknown")
        print(f"  [{task_id}] state={state}, elapsed={int(time.time()-start)}s")
        if state in ("success", "failed"):
            return task
        time.sleep(interval)
    raise TimeoutError(f"Task {task_id} not done in {max_wait}s")

# 示例：跑 T01 现代都市·角色一致性
if __name__ == "__main__":
    result = create_drama_task(
        script_name="T01 咖啡馆对话",
        script_content="""【镜头1｜0-3s｜中景】短发黑衣女孩坐在窗边低头看手机。
【镜头2｜3-6s｜中景】她抬头望向窗外，嘴角轻轻上扬。
【镜头3｜6-9s｜近景】她端起咖啡杯，轻抿一口，眼神柔和。""",
        assets=[
            {"name": "短发女孩", "type": "character", "image_path": "/path/to/girl.png", "description": "短发黑衣女孩"},
            {"name": "咖啡馆", "type": "scene", "image_path": "/path/to/cafe.png", "description": "暖黄灯光咖啡馆"}
        ],
        duration=0,  # 自动
        style="电影感写实"
    )
    print("Create result:", result)
    task_id = result.get("task_id") or result.get("task", {}).get("id")
    if task_id:
        final = poll_until_done(task_id)
        print("Final state:", final.get("meta", {}).get("state"))
        signed_url = final.get("derived_data", {}).get("signed_final_video_url")
        credits = final.get("derived_data", {}).get("deducted_credits")
        print(f"Credits: {credits}, Video URL: {signed_url}")
```

### 4.8 常见问题

| 问题 | 解决方案 |
|------|----------|
| ngrok 警告页无法跳过 | 换浏览器（Chrome/Edge/Firefox），或带 `ngrok-skip-browser-warning: 1` header 用 curl/Python 脚本访问 |
| `请先填写 Vidu API Key` 弹窗 | 顶部 API Key 输入框未填或被清空，重新粘贴即可 |
| `请填写剧本名称和内容` 弹窗 | 剧本名或内容为空，剧本名 ≤ 20 字、内容 ≤ 2000 中文字 |
| `请添加至少一个素材` 弹窗 | 未上传任何角色/场景素材 |
| 任务一直 pending | 测试期积分不足或 Vidu 服务排队，联系 Vidu 同学确认 |
| 任务 failed，err_code 显示 | 详情卡片会展示 `错误: {err_code} - {err_msg}`，截图发给 Vidu 同学排查 |
| 图片转 base64 后请求体过大 | 单张图建议 ≤ 5 MB（虽然 UI 限制 50 MB，但 POST 大 body 易超时） |
| 短剧 resolution 不可调 | UI 未暴露，代码硬编码 1080p，与官方 Q3-Drama 1080P 一致 |
| 任务列表刷新后还在 | localStorage 持久化，需手动点"清空"才会清除 |
| 导出 CSV 中文乱码 | 文件已带 BOM，用 Excel 打开应正常；若 WPS 打开乱码，用 VSCode 打开另存为 UTF-8 |
| 接口返回 401/403 | API Key 无效或积分耗尽，去 platform.vidu.cn/api-keys 确认 Key 状态 |
| 输出视频"脸油" | 替换参考图为自生成图（用 Midjourney/即梦/可灵文生图先做高清人物图），见方案 §2.3 T24 用例 |
| 长程生成中途超时 | 单镜最长 30s，长镜头拆分为多个 8-12s 分镜分别生成，后期剪辑拼接 |

### 4.9 评测执行建议

基于测试站实际能力，对评测方案的执行做如下调整：

1. **24 用例全部走短剧 API**：每个用例对应 1 个 `create_drama_task` 调用，`script_content` 写入分镜剧本，`assets` 上传角色/场景/道具图
2. **T21 长程稳定性**：10 集连续生成 = 10 个 task 串行 + 同时跑竞品对照，预估积分 50 task × 8s × 28 = 11200 积分（需提前申请）
3. **T24 参考图对照**：3 组参考图（网络图/自生成/高清自拍）分别跑同一提示词，比对"脸油"程度
4. **MV 生成作为软文亮点**：方案 B 软文可加一段"实测 MV 生成：从静态模特图 + 一段音频，30 秒生成对口型音乐视频"作为差异化展示
5. **批量任务管理**：利用测试站的"批量查询"+"导出 CSV"功能，避免手动逐条记录
6. **状态轮询节奏**：建议 15 秒一次（脚本化），人工查询可 1-2 分钟一次

---

## 5. 附录 A：评测日历

| 日期 | 任务 | 交付物 |
|------|------|--------|
| 2026-06-30 | 文档定稿 + API Key 申请 + 测试群对接 | 本文档 |
| 2026-07-01 | 积分到账 + T01 跑通 + 竞品基线 | 基线报告 |
| 2026-07-02 | T01-T12 跑完（现代都市+仙侠玄幻） | 12 用例数据表 |
| 2026-07-03 | T13-T20 跑完（悬疑+古装）+ T22-T24 跨题材 | 21 用例数据表 |
| 2026-07-04 | T21 长程测试 + 异常复测 | 长程降智曲线 |
| 2026-07-05 | 方案 A 评测报告定稿 + 内部评审 | 评测报告 v1.0 |
| 2026-07-06 | 方案 B 软文初稿 + 配图制作 | 软文初稿 |
| 2026-07-07 | 软文内部评审 + 与 Vidu 对齐 | 软文 v1.0 |
| 2026-07-08 | 软文发布 + Vidu 内部测试意见交付 | 发布链接 + 意见清单 |

---

## 6. 附录 B：测试用例提示词速查表

> 以下提示词均按 prompt_guide 规范编写，可直接复制使用。每个用例的"参考图说明"指明需要上传的图，未注明则纯文生。

### T01 现代都市·角色一致性（单人 3 镜头对话）

```
风格：电影感写实；景别：中景；机位：平视；构图：三分构图；运镜：固定镜头。
背景是咖啡馆白天暖黄灯光。短发黑衣女孩坐在窗边。
镜头一：她低头看手机，眉头微皱。
镜头二：她抬头望向窗外，嘴角轻轻上扬。
镜头三：她端起咖啡杯，轻抿一口，眼神柔和。
图1为[场景：咖啡馆]，图2为[角色：短发黑衣女孩]。
背景音乐：轻爵士钢琴；音效：咖啡机研磨声，杯碟轻碰声。
```
参考图：图1 咖啡馆场景图（自生成），图2 短发黑衣女孩半身像（自生成 1080P 正面照）

### T02 现代都市·双人对峙（过肩镜头）

```
风格：电影感写实；景别：中景；机位：平视。
背景是咖啡馆，两人坐着谈话。
镜头一是林曼的过肩镜头，全程保持静止镜头，林曼始终背对着镜头，她看了一眼手腕上的手表。
镜头二是周扬的中等镜头，镜头对准周扬，他表情淡定微笑。
图1为[场景：咖啡馆]，图2为[角色：林曼，长发白领]，图3为[角色：周扬，短发西装男]。
```
参考图：图1 咖啡馆，图2 林曼，图3 周扬（均自生成）

### T03 现代都市·口型对齐（Speech 短句）

```
风格：电影感写实；景别：近景特写。
短发女孩站在窗边望向远处，午后阳光透过玻璃。
她轻声说："终于等到你了。"语气温柔，略带哽咽，嘴角微微颤抖，眼眶泛光。
图1为[场景：窗边]，图2为[角色：短发女孩]。
背景音乐：钢琴独奏，舒缓。
```
参考图：图1 窗边场景，图2 短发女孩
**评测重点**：口型与"终于等到你了"6 字中文对齐度

### T06 现代都市·镜头叙事（5 镜头办公室）

```
风格：电影感写实。
【镜头1｜0-3s｜中景】男生坐在电脑前抓头发，满脸焦虑。男生："这方案半小时后就要交了！"
【镜头2｜3-5s｜中景】女生淡定走过来，把手机递给他。女生："用AI啊。"
【镜头3｜5-7s｜特写】电脑屏幕瞬间生成完整方案，屏幕光映在男生脸上。
【镜头4｜7-9s｜近景】男生瞪大眼睛："这么快？"
【镜头5｜9-12s｜中景】女生微笑倚在桌边："不然呢？"
图1为[场景：办公室]，图2为[角色：男生]，图3为[角色：女生]。
音效：键盘敲击声，手机提示音，环境空调低鸣。
```
参考图：图1 办公室，图2 男生，图3 女生

### T08 仙侠玄幻·大场面特效（火球→火蝶→火凤）

```
风格：仙侠玄幻电影级；运镜：环绕+俯拍推进。
[@图1] 切镜1：超特写，微距运镜，古风仙侠男子指尖凝聚出一点星火，星火逐渐膨胀化作燃烧的火球，镜头聚焦在指尖火焰，火焰纹理清晰可见，映出他冷峻的眼神。
切镜2：环绕运镜，男子将火球掷向空中，"去吧！"火球在空中炸裂化作无数火蝶，火蝶飞舞时留下金色轨迹，镜头环绕他，火蝶在他酒红华服周围盘旋。
切镜3：极远景，俯拍推进，火蝶汇聚成巨大的火凤，火凤展翅翱翔掠过云海，镜头跟随火凤高速推进，最终火凤撞向远方的山峰，引发剧烈爆炸，火焰染红半边天。
图1为[角色：古风仙侠男子，酒红华服]，图2为[场景：云海山峰]。
音效：火焰燃烧的呼啸，火蝶振翅的细碎声，火凤长鸣，撞击爆炸的轰鸣。
```
参考图：图1 古风男子（自生成），图2 云海山峰场景

### T11 仙侠玄幻·5 镜头史诗打戏

```
（按 prompt_guide 打戏 JSON 模板编写）
scene_info: 第3集高潮，夜晚古战场，承接第2集反派挑衅，主题：绝境反击
characters: 楚寒（重伤但战意燃烧，红眼光），徐震天（冷酷居高临下），楚云（旁观绝望）
opening_shot: 中景平视，楚寒从石壁中爬出，鲜血滴落
emotion_trigger: 看到楚云被威胁
emotion_explosion: 楚寒咬牙切齿："遗言？该留遗言的是你！"
power_up: 激活九霄云纹戒，金色力量涌入体内，手臂发出金光，战甲凝聚
skill_release: "九霄破天掌！"金色光掌从天而降
environment_feedback: 地面龟裂，碎石悬浮，金色光柱冲天
ending_shot: 仰视远景，金色光掌拍向徐震天，画面定格
audio: 骨骼脆响，能量轰鸣，金光爆发的冲击波，楚云绝望呼喊"少主！"
global_constraints: 角色一致性、场景一致性、禁止字幕
```
参考图：楚寒、徐震天、楚云三角色图 + 古战场场景图

### T18 古装宫斗·复杂情绪（"嫌弃"）

```
风格：古装宫斗剧；景别：近景推进；运镜：固定镜头+面部推进。
酒店宴会厅，王桂兰（画面右侧，坐在席位上）苏文博、许向暖（画面左侧远处，敬酒中）。
婚礼现场座无虚席，坐满宾客。苏文博的母亲王桂兰坐在席位上，眉头紧紧皱起，面露不满。
镜头固定，捕捉她皱眉的表情。
镜头推进王桂兰面部，她眼神阴沉，嘴角下撇，带着嫌弃和不满。强调她的不悦。
图1为[场景：宴会厅]，图2为[角色：王桂兰，中年贵妇]，图3为[角色：苏文博]，图4为[角色：许向暖]。
背景音乐：古筝琵琶，低沉压抑。
```
参考图：4 张角色/场景图
**评测重点**：模型对"嫌弃"这一复杂情绪的理解度（同事提到的 SD 弱项）

### T22 跨题材·负面提示词失效验证

```
# 对照组 A（含负面提示词）：
一位年轻护士站在医院走廊，不戴护士帽，身穿白色护士服，手持病历夹。
# 对照组 B（改写为视觉描述）：
一位年轻女性站在医院走廊，身穿白色 V 领短袖制服，手持病历夹，头发自然垂落，无任何头饰。
```
**评测重点**：A 组是否仍出现护士帽；B 组是否成功避开

### T24 跨题材·参考图质量影响（"脸油"问题对照）

```
同一提示词：
参考图中的男性，新场景：他站在东京街头夜晚，霓虹灯背景，回头微笑，眼神明亮。
```
三组参考图对照：
- A 组：网络下载的男性人像（典型"油腻感"网图）
- B 组：用 Midjourney/即梦自生成的男性人像（清爽质感）
- C 组：高清自拍（手机后置摄像头拍摄）

**评测重点**：复现同事提到的"脸油"问题，验证是否源于参考图本身

### T21 长程稳定性（10 集连续生成，如积分允许）

- 选定一个 10 集短剧剧本（建议现成漫剧 IP 改编）
- 每集 3-5 个分镜，共约 30-50 个生成任务
- 同一组角色参考图全程复用
- 每集生成后做面部一致性 CLIP-IQA 评分
- 画"集数-一致性分数"曲线，观察是否后段降智
- 积分预算：50 任务 × 8s × 28 积分 = 11200 积分（约 9800 元）— **需提前与 Vidu 沟通积分配额**

---

## 7. 附录 C：竞品对照基线（用于方案 A 横向对比）

| 维度 | Vidu Q3-Drama | Seedance 2.0 | Kling 3.0 | 即梦 AI |
|------|--------------|-------------|-----------|---------|
| 定价 | 28 积分/秒 (0.875 元/秒) | 较高 | 中 | 较低 |
| 最大时长 | 16s 声画同出 | 多模态联合 | 1080P 电影级 | 性价比 |
| 角色一致性 | 多主体稳定 | — | — | 5.8/10 (知乎评测) |
| 大场面特效 | 强冲击力 | 动态偏弱 | — | — |
| 画面美学 | 光影/质感好 | 锐化感重 | — | 颜色偏差 |
| 音视频直出 | ✅ 原生 | ✅ 多模态 | — | — |
| 多模态输入 | 参考生 | 文/图/音/视四模态 | — | — |
| 智能切镜 | ✅ | — | — | — |

> 表中"—"表示未在本轮测试中获取数据，方案 A 执行时按实际跑测结果填充。

---

## 8. 版本记录

| 版本 | 日期 | 修改 |
|------|------|------|
| v1.0 | 2026-06-30 16:30 | 初版，含方案 A + 方案 B + ngrok 使用说明 + 24 用例提示词库 |
| v2.0 | 2026-06-30 | §4 ngrok 操作指南基于实际 HTML 源码重写，新增短剧/MV 双流程、4 个 API 端点、Python 脚本 |

---

**文档结束**
